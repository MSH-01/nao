{
	"name": "nao-chat",
	"workspaces": [
		"apps/*"
	],
	"scripts": {
		"dev": "npm-run-all -lp dev:backend dev:frontend",
		"dev:backend": "npm run dev -w backend",
		"dev:frontend": "npm run dev -w frontend",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
